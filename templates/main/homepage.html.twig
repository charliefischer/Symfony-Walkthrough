{% extends 'base.html.twig' %}
{% block body %}
{% block title %}New Page Title Goes Here{% endblock%}
<h1 class="text-2xl">Hey World</h1>

<div>
  Browse our {{ ships|length }} different Starships.

  {% if ships|length > 2 %}
  {# is this too many #}
    Plenty of ships
  {% endif %}
</div>

<div>
  {{include('main/_shipStatus.html.twig')}}
</div>

<div>
  <table>
  {% for ship in ships %}
  <tr>
    <th>Name</th>
    <td>
    <a href="{{path('app_starship_show', {id: ship.id})}}">
    {{ship.name}}
    </a>
    </td>
  </tr>
  {% endfor %}
  </table>
  <img src="{{asset('images/queen_mandela.png')}}" />
</div>
{% endblock %}